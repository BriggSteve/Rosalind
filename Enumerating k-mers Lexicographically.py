from itertools import permutations

symbols = ["A", "B", "C", "D"]

for i in list(symbols):
    limited_symbols =


perms = [''.join(p) for p in permutations(symbols)]



print(perms)

values = [2]
# use scipy comb for this????

# loop permutation through new_list for x amount of times(x=values)

